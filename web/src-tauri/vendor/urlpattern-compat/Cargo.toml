[package]
name = "urlpattern"
version = "0.3.1"
edition = "2021"
license = "MIT"
description = "Compatibility bridge from Tauri's urlpattern 0.3 constraint to urlpattern 0.6"
publish = false

[dependencies]
urlpattern06 = { package = "urlpattern", version = "=0.6.0" }

[dev-dependencies]
regex = "1"
url = "2"
